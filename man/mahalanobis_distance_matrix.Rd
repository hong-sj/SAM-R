% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mahalanobis-utils.R
\name{mahalanobis_distance_matrix}
\alias{mahalanobis_distance_matrix}
\title{Pairwise Mahalanobis distance matrix}
\usage{
mahalanobis_distance_matrix(X_query, X_reference, S_inv)
}
\arguments{
\item{X_query}{Numeric matrix with observations in rows.}

\item{X_reference}{Numeric matrix with observations in rows.}

\item{S_inv}{Inverse covariance matrix.}
}
\value{
A numeric matrix of pairwise Mahalanobis distances.
}
\description{
Computes pairwise Mahalanobis distances between rows of `X_query`
and `X_reference` using the supplied inverse covariance matrix.
}
\examples{
data(sample_4group)

covariates <- setdiff(
  names(sample_4group),
  c("synthetic_id", "treatment", "mortality_28d")
)
groups <- unique(as.character(sample_4group$treatment))

pooled <- get_pooled_covariance(
  sample_4group,
  covariates,
  "treatment"
)

X1 <- as.matrix(
  sample_4group[sample_4group$treatment == groups[1],
                covariates, drop = FALSE]
)
X2 <- as.matrix(
  sample_4group[sample_4group$treatment == groups[2],
                covariates, drop = FALSE]
)

mahalanobis_distance_matrix(
  X1[1:5, , drop = FALSE],
  X2[1:5, , drop = FALSE],
  pooled$S_inv
)
}
