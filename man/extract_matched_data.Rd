% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sam_evaluate.R
\name{extract_matched_data}
\alias{extract_matched_data}
\title{Extract matched subject-level data}
\usage{
extract_matched_data(
  data,
  search,
  match_result,
  treatment_var = "T",
  anchor_level = NULL
)
}
\arguments{
\item{data}{Original `data.frame` used for matching.}

\item{search}{Output from [gps_candidate_search()].}

\item{match_result}{Output from [sam_match()].}

\item{treatment_var}{Name of the treatment variable. Default `"T"`.}

\item{anchor_level}{Optional anchor treatment group. If `NULL`, it is
inferred from `search`.}
}
\value{
A subject-level `data.frame` containing the matched cohort with
  `matched_set_id`, `matched_role`, and `original_row` appended.
}
\description{
Reconstructs the subject-level matched cohort from a completed SAM match.
Each retained subject is assigned its matched-set identifier, treatment
role, and original row index.
}
\examples{
data(sample_4group)

covariates <- setdiff(
  names(sample_4group),
  c("synthetic_id", "treatment", "mortality_28d")
)
anchor <- unique(as.character(sample_4group$treatment))[1]

fit <- estimate_gps_multinom(
  sample_4group,
  X_vars = covariates,
  treatment_var = "treatment",
  anchor_level = anchor
)

search <- gps_candidate_search(
  sample_4group,
  fit$gps,
  treatment_var = "treatment",
  anchor_level = anchor,
  top_m = 10,
  gps_space = "logit"
)

match_result <- sam_match(
  sample_4group,
  search,
  X_vars = covariates,
  treatment_var = "treatment"
)

matched_data <- extract_matched_data(
  sample_4group,
  search,
  match_result,
  treatment_var = "treatment",
  anchor_level = anchor
)

head(matched_data)

}
