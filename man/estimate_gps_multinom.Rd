% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gps_multinom.R
\name{estimate_gps_multinom}
\alias{estimate_gps_multinom}
\title{Estimate generalized propensity scores}
\usage{
estimate_gps_multinom(
  data,
  X_vars = paste0("X", 1:10),
  treatment_var = "T",
  anchor_level = "A"
)
}
\arguments{
\item{data}{A `data.frame` containing treatment and covariate variables.}

\item{X_vars}{Character vector of covariate column names.
Default `paste0("X", 1:10)`.}

\item{treatment_var}{Name of the treatment variable. Default `"T"`.}

\item{anchor_level}{Reference treatment group. Default `"A"`.}
}
\value{
A list containing the fitted multinomial model (`model`) and
  predicted treatment probabilities (`gps`).
}
\description{
Estimates generalized propensity scores using multinomial logistic
regression with `anchor_level` as the reference treatment group.
}
\examples{
data(sample_4group)

covariates <- setdiff(
  names(sample_4group),
  c("synthetic_id", "treatment", "mortality_28d")
)
anchor <- unique(as.character(sample_4group$treatment))[1]

fit <- estimate_gps_multinom(
  sample_4group,
  X_vars = covariates,
  treatment_var = "treatment",
  anchor_level = anchor
)

head(fit$gps)
rowSums(fit$gps)[1:5]

}
