% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mahalanobis-utils.R
\name{build_group_distance_matrices}
\alias{build_group_distance_matrices}
\title{Build Mahalanobis distance matrices by comparator group}
\usage{
build_group_distance_matrices(data, X_vars, treatment_var, anchor_rows, groups)
}
\arguments{
\item{data}{A `data.frame` containing covariate and treatment variables.}

\item{X_vars}{Character vector of covariate column names.}

\item{treatment_var}{Name of the treatment variable.}

\item{anchor_rows}{Row indices of anchor subjects.}

\item{groups}{Character vector of comparator treatment groups.}
}
\value{
A list containing the inverse covariance matrix (`S_inv`),
  comparator-group row indices (`group_rows`), and Mahalanobis distance
  matrices (`D`).
}
\description{
Computes Mahalanobis distance matrices between anchor subjects and
subjects in each comparator treatment group.
}
\examples{
data(sample_4group)

covariates <- setdiff(
  names(sample_4group),
  c("synthetic_id", "treatment", "mortality_28d")
)

treatment_levels <- unique(
  as.character(sample_4group$treatment)
)
anchor <- treatment_levels[1]
groups <- setdiff(treatment_levels, anchor)

anchor_rows <- which(
  sample_4group$treatment == anchor
)

built <- build_group_distance_matrices(
  sample_4group,
  X_vars = covariates,
  treatment_var = "treatment",
  anchor_rows = anchor_rows,
  groups = groups
)

dim(built$D[[groups[1]]])

}
